require("blink").setup({
   show_parameter_hints = true,          -- show function parameter hints
   show_type_hints = true,               -- show type hints
   hints_prefix = "» ",                  -- prefix for the hints
   highlight = "Comment",                -- highlight group
})
